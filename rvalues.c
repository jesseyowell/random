#include <stdio.h>

int main(void) {

    int fstep, bstep;
    float nstep, mstep;

    printf("int values are: %d %d\n", fstep, bstep);
    printf("float values are: %f %f\n", nstep, mstep);

    return 0;
}